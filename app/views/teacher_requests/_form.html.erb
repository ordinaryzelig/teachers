<%= form_for(teacher_request) do |f| %>
  <% if teacher_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teacher_request.errors.count, "error") %> prohibited this teacher_request from being saved:</h2>

      <ul>
      <% teacher_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :teaching_position_id %>
    <%= f.number_field :teaching_position_id %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :closed_at %>
    <%= f.datetime_select :closed_at, :include_blank => true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
